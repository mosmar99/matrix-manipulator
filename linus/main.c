#include "intarray.h"

int main()
{
    /*
    intArray a = {2, 6, 8, 4, 5, SENTINEL};
    printIntArray(a);

    intArray a = {2, 6, 8, SENTINEL};
    printf("%u\n", getIntArraySize(a));

    intArray a = {0};
    bool b = getIntArray(c);
    
    printf("%d\n", checkCStr("1,2,3"));
    */

    intArray a = {0};
    bool b = getIntArray(a);
    printIntArray(a);
}